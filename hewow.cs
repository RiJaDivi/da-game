string[][] map = new map[maple][maple];
Console.Writeline("hewow");
maple = 8
 int x = 0;
  int y = 0;



for (int i = 0; i < maple; i++){
  for (int j = 0; j < maple; j++){
map[i][j] = .
  }
}

static string Plaher(int x,int y) {
map[x][y] = P
 }

static string Maprint(string map[][]) {
    for (int i = 0; i < maple; i++)
    {
        for (int j = 0; j < maple; j++)
        {
            Console.Write(pole[i][j] + " ");
        }
        Console.WriteLine();
    }
}
    var key = Console.ReadKey(true);


